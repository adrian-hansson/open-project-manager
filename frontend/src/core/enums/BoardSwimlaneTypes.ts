export enum BoardSwimlaneTypes {
    /** Display no swimlanes. */
    None = 0,
    /** Display swimlanes based on a specific issue type level. */
    IssueTypeLevel = 1,
    /** Display swimlanes based on issue type. */
    IssueType = 2,
    /** Display swimlanes based on assignee. */
    Assignee = 3,
    /** Display swimlanes based on team. */
    Team = 4
}
